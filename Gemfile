source 'https://rubygems.org'

if (secret = ENV['RAILS_LTS_SECRET'])
  source "https://#{secret}@gems.railslts.com" do
    gem 'rails', '~> 3.2'
    gem 'activerecord'
    gem 'activesupport'
  end
end

gemspec